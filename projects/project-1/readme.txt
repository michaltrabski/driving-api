1 check if all pages have been created with correct slugs as in poznaj-testy.pl
2 check if all pages have content at least the same as in original poznaj-testy.pl
3 check if all pages have correct title
4 check if login / register is working
5 add all media with correct size png and mp4
6 check if user clicks are stored in firebase
7 check if user can buy access

